const audioDir = "/audios/"
const dracoDir = "/draco/"
const fontsDir = "/fonts/"
const hdrsDir = "/hdrs/"
const imagesDir = "/images/"
const modelsDir = "/models/"
const svgDirDir = "/svgs/"

export const Files = {

	AUDIOS: {
		BACKGROUND: audioDir + "background.mp3",
		HOVER_BUTTON: audioDir + "hover-button.mp3",

	},

	DRACO: {
		GLTF: dracoDir + "gltf/"
	},

	FONTS: {

	},

	HDRS: {
		SPACE: hdrsDir + "space.hdr"
	},

	IMAGES: {
		DESIGNS: imagesDir + "designs.png",
		FAVICON: imagesDir + "favicon.ico",
		LOGO_MIN: imagesDir + "logo-min.png"
	},

	MODELS: {
		CAR_CITY: modelsDir + "CarCity.glb",
		ASTRO_YORKIE: modelsDir + "AstroYorkie.glb"
	},

	SVGS: {
		LOGO_MAX: svgDirDir + "logo-max.svg",
	}

}